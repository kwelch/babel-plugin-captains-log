language: node_js
cache:
  directories:
    - ~/.npm
node_js:
  - "node"
  - "10"
  - "12"
script:
  - yarn build
  - yarn test --coverage
after_success:
  - yarn report-coverage
  - yarn travis-deploy-once "yarn semantic-release"
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
