language: node_js
cache:
  directories:
    - ~/.npm
node_js:
  - "node"
  - "10"
  - "12"
script:
  - yarn test --coverage
  - yarn build
after_success:
  - yarn report-coverage
  - yarn travis-deploy-once "yarn semantic-release"
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
